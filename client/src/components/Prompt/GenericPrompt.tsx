import Prompt from '.';

const GenericPrompt = () => {
  return (
    <Prompt
      title="Don't miss what's happening"
      description='People on Chirper are the first to know.'
    />
  );
};

export default GenericPrompt;
